import React, { Fragment } from "react";
import { Button } from "react-bootstrap";

const Cloud = () => {
  return (
    <Fragment>
      <div className="cloud">
        <h1 style={{ margin: "0.5em" }}>Connect</h1>
      </div>
    </Fragment>
  );
};

export default Cloud;
